{
  "kind": "processing-rule",
  "schemaVersion": 1,
  "id": "map_censtatd_district_code_to_canonical_division",
  "scope": "bulk",
  "basis": "code",
  "summary": "Match each selected C&SD district code with its corresponding Home Affairs Department district.",
  "inputs": ["identity-mappings", "district-cohort"],
  "outputs": ["canonical-district-identities"],
  "parameters": {
    "sourceAuthority": "hkgov-censtatd",
    "targetAuthority": "hkgov-had",
    "targetCohort": "2022",
    "domain": "administrative"
  },
  "implementation": {
    "path": "apps/harbour-cli/src/lib/statisticsSql/censtatdDistrictBridge.ts",
    "symbol": "censtatdDistrictIdentityRule"
  }
}
