{
  "kind": "processing-rule",
  "schemaVersion": 1,
  "id": "normalise-als-addresses",
  "scope": "bulk",
  "basis": "code",
  "summary": "Make the address fields consistent in both languages. Apply reviewed field corrections and identify whether each address describes a whole site, a building or a section.",
  "inputs": ["prepared-addresses"],
  "outputs": ["address2d", "address2dI18n"],
  "parameters": {},
  "implementation": {
    "path": "libs/core/src/pipeline/services/addressPipeline/normalisation.ts",
    "symbol": "addressNormalisationRule"
  }
}
