{
  "name": "basemap-tiles",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --var AUTH_MODE:disabled --var ENVIRONMENT:local",
    "dev:auth": "wrangler dev --var AUTH_MODE:required --var ENVIRONMENT:local",
    "build": "mkdir -p dist && wrangler deploy --dry-run --outdir dist --minify",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "check": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^1.3.1",
    "@repo/auth": "workspace:*",
    "@repo/basemap": "workspace:*",
    "pbf": "^3.3.0",
    "pmtiles": "^4.4.1",
    "vt-pbf": "^3.1.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pbf": "^3.0.5",
    "bun-types": "^1.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
