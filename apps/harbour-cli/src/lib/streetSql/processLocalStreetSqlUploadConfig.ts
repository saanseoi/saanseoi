import { streetLocaleCodes } from '@repo/db'

export const LOCAL_RELEASE_ROOT = `${import.meta.dir}/../../../../../.local/harbour-sql/releases`

export const LANDSD_STREET_SNAPSHOT_SOURCE_ROLE = 'primary'

export const REQUIRED_LOCALES = streetLocaleCodes
