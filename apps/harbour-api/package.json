{
  "name": "harbour-api",
  "type": "module",
  "scripts": {
    "dev": "bash ../../scripts/dev-local-stack.sh",
    "build": "mkdir -p dist && wrangler deploy --dry-run --env production --outdir dist --minify",
    "build:preview": "mkdir -p dist && wrangler deploy --dry-run --env preview --outdir dist --minify",
    "build:production": "mkdir -p dist && wrangler deploy --dry-run --env production --outdir dist --minify",
    "deploy": "wrangler deploy --env production --minify",
    "deploy:preview": "wrangler deploy --env preview --minify",
    "deploy:production": "wrangler deploy --env production --minify",
    "check": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "test": "bun test",
    "test:ci": "bun test --max-concurrency=1 src/lib/services/control.test.ts src/lib/services/uploadSession.localProcessingOwnership.test.ts && bun test --path-ignore-patterns='**/control.test.ts' --path-ignore-patterns='**/uploadSession.localProcessingOwnership.test.ts'",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.4.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.6.1",
    "@aws-sdk/client-s3": "^3.1121.0",
    "@aws-sdk/s3-request-presigner": "^3.1121.0",
    "@repo/core": "workspace:*",
    "@repo/db": "workspace:*",
    "@scalar/hono-api-reference": "^0.12.0",
    "@scalar/openapi-to-markdown": "^0.5.43",
    "hono": "^4.13.5",
    "zod": "^4.5.4"
  }
}
